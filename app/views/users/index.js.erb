function refreshCards(innerHTML) {
  newCardsContainer = document.querySelector(".container-holder")
  newCardsContainer.innerHTML = innerHTML;
 }

refreshCards('<%= j render "users", therapists: @therapists %>');

$("#modal-window").find(".modal-content").html("<%= j (render 'new') %>");
$("#modal-window").modal();
