function refreshCards(innerHTML) {
  newCardsContainer = document.querySelector(".cards-container")
  newCardsContainer.innerHTML = innerHTML;
}

refreshCards('<%= j render "users", therapists: @therapists %>')
